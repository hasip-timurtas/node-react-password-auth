export interface UserInterface {
    username: string,
    password: string,
    isAdmin: boolean,
    _id: string
}

export interface UserInfo {
    id: string,
    username: string,
    isAdmin: boolean
}